# AI Audit Log

## Audit Table

| Task | AI Used | Accepted | Modified | Rejected | Why |
|------|---------|----------|----------|----------|-----|
| Generate database schema (users, sessions, attendance_records, leave_requests) | Claude | Yes | Added `version` column manually for OCC | - | AI generated correct base schema but missed versioning for optimistic concurrency |
| Write seed SQL with realistic attendance data | ChatGPT | Partial | Changed attendance percentages to ensure some students fall below 75% | - | AI generated uniform data; needed realistic variation for warning system demo |
| CSV import backend logic | Claude | Partial | Had to fix idempotency — AI did not check for existing records before inserting, always counted all rows as "imported" | Skipped count logic | AI missed the skipped vs imported distinction entirely |
| Generate sample_attendance.csv with 50+ rows | Claude | Modified | Had to fix column name from `student_id` to `roll_no`, fix status `late` to `leave`, and use only valid roll numbers from DB | Initial file | AI used wrong column names and invalid status values not matching assignment spec |
| ConflictResolver UI component (Reload Latest, Retry Update, Compare Changes) | Claude | Yes | Minor styling tweaks | - | AI correctly implemented all 3 required conflict resolution options |
| Optimistic concurrency control — version-based update logic | Claude | Yes | Added error propagation for `err.attempted` in bulk mark | - | AI correctly implemented OCC but missed passing attempted record in bulk conflict error |
| Faculty tripwire — reject unauthorized session access | ChatGPT | Yes | - | - | AI correctly identified the pattern; implementation matched requirement |
| `/metrics` endpoint with count, avg_ms, p95_ms | Claude | Yes | - | Prometheus suggestion | AI suggested using Prometheus; kept in-memory tracker as simpler solution for this scope |
| Draft recovery using browser storage | Codex | Partial | Changed from localStorage to IndexedDB via custom hook | localStorage approach | Codex used localStorage which resets on some browsers; switched to more reliable storage |
| Search and filter on students page | ChatGPT | Yes | Added attendance range filter (min/max) which AI did not include | Missing filter | AI implemented name/roll search and department/semester filters but missed attendance range |

---

## Notes

- At least one AI output was wrong: **CSV import idempotency** — Claude did not implement the skipped count correctly, always reporting all rows as "imported" even on duplicate uploads. This was caught by manually uploading the same file twice and observing the count.
- The `sample_attendance.csv` generated by AI had wrong column names (`student_id` instead of `roll_no`) and invalid status values (`late` instead of `leave`), requiring manual correction.
